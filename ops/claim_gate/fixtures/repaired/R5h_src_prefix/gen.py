"""R5h REPAIRED -- the generator string is now a TRUE restatement.

It carries no figure the rendered page does not already carry and already
attribute, so src_dup clears it as the double-count it is, exactly as it
clears NEG15. The defect removed is the EXTRA, unrendered, unsourced figure --
not the settling sentence, which is correct copy and stays. This docstring is
not a surface.
"""

SETTLING_BLOCK = (
    '<p>Loose-fill cellulose settles 10 to 20 percent over its functional '
    'life, so reputable installers blow extra depth above the target.</p>'
)
